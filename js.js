const nomor = [2, 3, 20, 21];
let genap = [];
	let ganjil = [];
	let i = 0;

		for(i; i <nomor.length; i++){
			return "Invalid Data";
		}
        if (nomor[i] % 2 == 0) {
            genap.push(nomor[i]);
        }else{
            ganjil.push(nomor[i]);
        }
console.log(genap);
console.log(ganjil);

